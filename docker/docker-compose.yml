version: '3.8'

# Docker Compose para TFM - Demo SonarQube con PostgreSQL
# Configuración optimizada para análisis de seguridad

services:
  # Base de datos PostgreSQL para SonarQube
  postgres:
    image: postgres:15-alpine
    container_name: tfm-sonarqube-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: sonar
      POSTGRES_PASSWORD: sonar
      POSTGRES_DB: sonarqube
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - sonar-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sonar"]
      interval: 10s
      timeout: 5s
      retries: 5

  # SonarQube LTS (Long Term Support) Community Edition
  sonarqube:
    image: sonarqube:lts-community
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    container_name: tfm-sonarqube
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      # Configuración de base de datos
      SONAR_JDBC_URL: jdbc:postgresql://postgres:5432/sonarqube
      SONAR_JDBC_USERNAME: sonar
      SONAR_JDBC_PASSWORD: sonar
      
      # Configuración de recursos
      SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: 'true'
      
      # Configuración de seguridad
      SONAR_FORCEAUTHENTICATION: 'true'
      
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_extensions:/opt/sonarqube/extensions
      - sonarqube_logs:/opt/sonarqube/logs
    ports:
      - "9000:9000"
    networks:
      - sonar-network
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9000/api/system/status | grep -q '\"status\":\"UP\"' || exit 1"]
      interval: 30s
      timeout: 10s
      start_period: 120s
      retries: 3

volumes:
  postgres_data:
    driver: local
  sonarqube_data:
    driver: local
  sonarqube_extensions:
    driver: local
  sonarqube_logs:
    driver: local

networks:
  sonar-network:
    driver: bridge
