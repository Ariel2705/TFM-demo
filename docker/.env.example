# Variables de entorno para Docker Compose
# Copiar este archivo a .env y ajustar valores

# Credenciales de PostgreSQL
POSTGRES_USER=sonar
POSTGRES_PASSWORD=sonar
POSTGRES_DB=sonarqube

# Credenciales de SonarQube (por defecto)
# Usuario: admin
# Password: admin (cambiar en primer login)

# Puerto de SonarQube
SONARQUBE_PORT=9000

# Token de admin (generar después del primer login)
SONAR_TOKEN=
